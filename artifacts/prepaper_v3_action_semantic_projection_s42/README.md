# Reference-style V3 cluster projections

Each figure contains PCA and t-SNE panels with projected-space centroids. DAGs labelled Mixed (exact plurality ties) are omitted to match the requested non-tied presentation. The action and semantic figures are projected separately because they retain different non-tied subsets; they are visualizations, not the source of the original-space quantitative metrics.
